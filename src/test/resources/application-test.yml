spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/studyroom
    username: postgres
    password: 12345678
  flyway:
    enabled: true
    baseline-on-migrate: true
  jpa:
    hibernate:
      ddl-auto: create
      show-sql: true